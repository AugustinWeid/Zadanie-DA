"""
Задание 3: Факторизация
Реализовать функцию (или тело функции), которая при введении натурального числа n разбивает
его на простые множители (представить его в виде простых чисел).
# Пример:
n = 56
print(prime_factors(n))
# Вывод:
[2, 2, 2, 7]
Оценить оптимальность решения по времени и памяти и прикрепить текст кода.
"""

def prime_factors(n: int):
    """
    Возвращает список простых множителей натурального числа n.
    Множители располагаются в порядке возрастания, повторяются, если входят в разложение несколько раз.
    """
    if n <= 1:
        return []  # для 1 и 0 возвращаем пустой список (по условию n натуральное, значит n>=1, но для 1 нет простых множителей)

    factors = []
    # Обрабатываем делитель 2 отдельно, чтобы потом перебирать только нечётные числа
    while n % 2 == 0:
        factors.append(2)
        n //= 2

    # Перебираем нечётные делители от 3 до sqrt(n)
    d = 3
    while d * d <= n:
        while n % d == 0:
            factors.append(d)
            n //= d
        d += 2  # увеличиваем на 2, чтобы оставаться на нечётных

    # Если после всех делений осталось число больше 1, то оно простое
    if n > 1:
        factors.append(n)

    return factors


if __name__ == "__main__":
    # Интерактивный ввод числа
    try:
        n = int(input("Введите натуральное число: "))
        if n < 1:
            print("Число должно быть натуральным (>=1).")
        else:
            result = prime_factors(n)
            print(f"Простые множители числа {n}: {result}")
    except ValueError:
        print("Ошибка: введите целое число.")
